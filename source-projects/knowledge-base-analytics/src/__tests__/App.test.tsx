import{render,screen}from"@testing-library/react";import{QueryClient,QueryClientProvider}from"@tanstack/react-query";import App from"../App";test("renders dashboard",async()=>{const q=new QueryClient({defaultOptions:{queries:{retry:false}}});render(<QueryClientProvider client={q}><App/></QueryClientProvider>);expect(await screen.findByText("Knowledge Base Analytics")).toBeInTheDocument();expect(await screen.findByText("Getting Started with Open")).toBeInTheDocument()});